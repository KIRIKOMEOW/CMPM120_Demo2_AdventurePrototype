这个项目的“开发边界”可以分成两层来看：一层是老师希望你做成什么样的作业，另一层是现有代码实际上已经帮你搭好了什么、又限制了什么。

先说结论：你完全可以做“文字冒险”方向，但更准确地说，最适合这套模板的不是纯命令行式文字冒险，而是“点触式文字冒险 / 视觉化互动小说 / point-and-click adventure”。也就是：每个地点是一张场景，场景里有若干可点击对象，鼠标移上去会显示文字，点击会触发叙事、拿道具、切换地点、播放小动画。这和老师给的框架是高度匹配的。

**作业规定的边界**
从 [README.md](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/README.md:3>) 和你发的课件截图里，老师其实已经把目标定得很清楚了：

- 你至少要有 `4+` 个基于 `AdventureScene` 的场景，也就是至少 4 个“地点”场景。
- 你还要有 `2+` 个不是 `AdventureScene` 的场景，通常最自然的就是开始页、结局页、过场页、说明页之类。
- 你必须做 `2+` 个“引擎增强”，也就是不是只写内容，还要对 `adventure.js` 这种小框架本身做扩展，让后续写场景更方便。
- 大多数场景里要有 `2+` 个交互物体。
- 很多物体要有 `pointerover` 提示，也就是悬停描述。
- 很多物体要有 `pointerdown` 效果，也就是点击后有反馈。
- 一些物体还要有动画。

这说明老师期待的不是“只有文字、没有空间感”的纯小说阅读器，而是“有地点、有物件、有悬停提示、有点击反馈”的冒险游戏。

**现有代码已经给你的东西**
真正重要的是 [adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:17>)。它已经替你实现了这几个核心系统：

- 统一的场景 UI 布局，包括右侧信息栏。[adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:57>)
- 一个消息框，你可以用 `showMessage` 显示提示文本。[adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:97>)
- 一个跨场景保留的背包系统，但目前背包内容只是“字符串列表”。[adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:25>), [adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:146>)
- 场景切换与淡入淡出。[adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:210>)
- 一个约定：你主要应该在 `AdventureScene` 子类的 `onEnter()` 里写地点内容，而不是重写 `create()`。[adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:40>), [adventure.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/adventure.js:218>)

而 [game.js](</f:/UCSC/CMPM120/Demo2/AdventurePrototype/game.js:1>) 只是示范了怎么用这个小引擎：放一个物体，给它悬停文字，点击后播放 tween，或者拿到钥匙、开门、切场景。

所以老师给的源码不是在“限制你只能做 demo 里的内容”，而是在“规定你最好沿着这个模式扩展”：地点场景、对象交互、消息提示、背包、场景切换。

**它真正限制你的地方**
这套模板的限制主要不是 Phaser 的限制，而是 `AdventureScene` 这个小框架的设计取向：

- 它默认把游戏理解成“一个个地点场景”的结构，不是连续移动的大地图。
- 它默认交互方式是鼠标悬停和点击，不是键盘输入解析器。
- 它自带的状态只有“背包字符串”，没有更复杂的剧情变量、角色好感、任务状态、分支标记。
- 重新进入一个场景时，场景里的局部状态通常会重建；现在天然会跨场景保留的只有 inventory，别的剧情状态如果要保留，你得自己扩展。
- 右侧 UI 栏占了屏幕大约四分之一宽度，所以左边更像“摆放交互对象的舞台”，不是全屏小说文本框。
- 没有内建的对话树、存档系统、文字逐字机、选项菜单系统，这些都要你自己加。

这意味着：你可以做文字冒险，但如果你想做成“玩家键盘输入 `look key`、`use key on door` 的 parser game”，现有模板几乎没帮到你多少，你会走出作业原本鼓励的轨道。相反，如果你做成“点击房间里的物体触发文本和选择”的形式，就非常合适。

**你这个想法最适合落成什么样**
基于这套代码，最自然、也最稳妥的成品是这种类型：

- 4 到 6 个地点场景
- 每个地点有 2 到 5 个可交互对象
- 悬停时给短描述
- 点击时触发一段文字、动画、拿道具、消耗道具或切换地点
- 用少量背包物品串起谜题
- 加 1 到 3 个非 `AdventureScene` 场景做开场、结局、分支结局或说明页

以下是游戏制作的脚本：

Bad end:（雪莉被看守拿镰刀审判，我做好了两张审判时的插图，badend就是播放两张插图，二者淡入淡出过渡，应该有大约0.1秒的重叠的时间，然后背景淡出，Bad End 两个白色大字出现在屏幕中间）
Scene0:主菜单 
文本: 
START（鼠标悬浮在字体上时字体应该有浮起来的效果，并且有阴影和光照，起到强调已选中的效果）
背景: (监狱外景)
BGM:待定
 
Scene1:
文本:
1.介绍主角(橘雪莉)因为是魔女,拥有怪力魔法,就是力气特别大，可以一只手捏碎一个苹果的那种, (考虑插入两个占画面比较小的位置，在文本框位置的右上淡入，插画显示手拿苹果到捏碎苹果的过程)，因为被人类害怕而举报，所以进入到了这个魔女监狱。
2.因为在吃饭时不小心把狱友的餐盘的砸碎了，所以现在要被送去禁闭室
背景：牢房，有看守路过的牢房，
场景1：牢房，文本1
场景2：有看守路过的牢房，文本2，选项1：听看守的话，选项2：用怪力魔法揍看守，选项2会直接触发Bad end（雪莉被看守拿镰刀审判，我做好了两张审判时的插图，badend就是播放两张插图，二者淡入淡出过渡，应该有大约0.1秒的重叠的时间，然后背景淡出，Bad End 两个白色大字出现在屏幕中间）

BGM：待定
后续scene待定

